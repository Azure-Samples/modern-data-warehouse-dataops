# Databricks notebook source
dbutils.fs.mounts()

# COMMAND ----------

dbutils.fs.mount(
  source = "wasbs://sample-container@${DEPLOYMENT_PREFIX}asa01.blob.core.windows.net",
  mount_point = "/mnt/sample-container",
  extra_configs = {"fs.azure.account.key.${DEPLOYMENT_PREFIX}asa01.blob.core.windows.net":dbutils.secrets.get(scope = "storage_scope", key = "storage_account_key1")})

# COMMAND ----------

df = spark.read.json("/mnt/sample-container/sample-data.us-population.json")
display(df)