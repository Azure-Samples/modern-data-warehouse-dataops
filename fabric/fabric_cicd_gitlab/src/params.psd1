@{
    baseUrl = "https://api.fabric.microsoft.com/v1" # the base URL of the Fabric APIs
    fabricToken = "<fabric-token>" # the Fabric user token used to authenticate against the Fabric APIs
    workspaceName = "<ws-connected-to-dev-branch>" # the workspace name connected to the dev branch. If it doesn't exist, it will be created
    folder = "<local-fabric-item-defs-folder>" # the folder name (existing) where Fabric item files will be stored
    capacityId = "<capacity-id>" # the capacity id (existing) of the capacity where the Fabric item will be created
}