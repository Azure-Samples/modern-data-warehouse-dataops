name: earnings-per-share
# init_args:
#   db_question_id: <db_question_id>
evaluation:
  evaluators:
    fuzzy_match:
      evaluator_config:
        column_mapping:
          truth: ${data.inputs.truth.earnings-per-share.citations}
          expected_match: ${data.inputs.truth.earnings-per-share.expected_match}
    exact_numeric_match:
      evaluator_config:
        column_mapping:
          truth: ${data.inputs.truth.earnings-per-share.citations}
          expected_match: ${data.inputs.truth.earnings-per-share.expected_match}
  tags:
    question: earnings-per-share
