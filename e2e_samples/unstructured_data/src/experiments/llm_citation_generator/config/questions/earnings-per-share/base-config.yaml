name: earnings_per_share
# init_args:
#   db_question_id: <db_question_id>
evaluation:
  evaluators:
    fuzzy_match:
      evaluator_config:
        column_mapping:
          truth: ${data.inputs__truth__earnings_per_share}
    exact_numeric_match:
      evaluator_config:
        column_mapping:
          truth: ${data.inputs__truth__earnings_per_share}
  tags:
    question: earnings_per_share
