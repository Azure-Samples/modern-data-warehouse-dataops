name: earnings_per_share
# init_args:
#   db_question_id: <db_question_id>
evaluation:
  evaluators:
    fuzzy_match:
      evaluator_config:
        column_mapping:
          truth: ${data.inputs__truth__earnings_per_share.citations}
          expected_match: ${data.inputs__truth__earnings_per_share.expected_match}
    exact_numeric_match:
      evaluator_config:
        column_mapping:
          truth: ${data.inputs__truth__earnings_per_share.citations}
          expected_match: ${data.inputs__truth__earnings_per_share.expected_match}
  tags:
    question: earnings_per_share
