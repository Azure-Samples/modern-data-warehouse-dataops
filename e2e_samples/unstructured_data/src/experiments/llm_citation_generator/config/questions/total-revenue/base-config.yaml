name: total_revenue
init_args:
  db_question_id: 1
evaluation:
  evaluators:
    fuzzy_match:
      evaluator_config:
        column_mapping:
          truth: ${data.inputs__truth__total_revenue__citations}
          expected_match: ${data.inputs__truth__total_revenue__expected_match}
    exact_numeric_match:
      evaluator_config:
        column_mapping:
          truth: ${data.inputs__truth__total_revenue__citations}
          expected_match: ${data.inputs__truth__total_revenue__expected_match}
  tags:
    question: total_revenue
